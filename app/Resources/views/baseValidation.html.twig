<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Click and Cheers{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" type="text/css" href="{{ asset('css/bootstrap.css')}}">
            <link rel="stylesheet" type="text/css" href="{{ asset('css/validation.css')}}">
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>        
        <div class="container">
            <div class="row">
                <div class="col-md-6 col-md-offset-3 col-xs-12">
                    <div class="col-xs-12 noPadding validationLogo">
                        <img class="cacLogo" src="{{ asset('img/LogoClickandcheers.svg') }}" alt="Click and Cheers">
                    </div>
                    <div class="col-xs-12 validationHeader">
                        {% block header %}
                        {% endblock %}
                    </div>
                    <div class="col-xs-12 validationContent">
                        {% block content %}
                        {% endblock %}
                    </div>
                </div>
            </div>
        </div>        
    </body>
</html>
